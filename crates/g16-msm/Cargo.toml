[package]
name = "g16-msm"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
g16-field.workspace = true
ark-ff.workspace = true
ark-ec.workspace = true
rayon.workspace = true
# `accel`'s error type only. The rest of the crate is infallible.
thiserror.workspace = true

[dev-dependencies]
# test_rng and UniformRand for the oracles; tests only, nothing in the library path.
ark-std.workspace = true
# field_cpu_throughput needs a concrete curve to time; tests only.
ark-bn254.workspace = true
